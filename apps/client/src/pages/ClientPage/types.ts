export type CreatedSdkKeyState = {
  configId: string;
  environmentId: string;
  key: string;
  projectId: string;
};

export type MemberFormValues = {
  email?: string;
  userId?: string;
  role: string;
};
